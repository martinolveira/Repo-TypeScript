// enum
enum Roles {
    User = 'User',
    Admin = 'Admin',
    SuperAdmin = 'SuperAdmin'
}

console.log(Roles.Admin);

// objects
const roles = {
    User: 0,
    Admin: 1,
    SuperAdmin: 2
};

console.log(roles.Admin);